"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setDeprecated = setDeprecated;
/**
 * @module node-opcua-utils
 */
// tslint:disable:ban-types
const node_opcua_assert_1 = require("node-opcua-assert");
/* c8 ignore next */
function setDeprecated(constructorFunc, methodName, helpString) {
    const oldMethod = constructorFunc.prototype[methodName];
    (0, node_opcua_assert_1.assert)(oldMethod instanceof Function, `expecting a valid ${methodName}method on class ${constructorFunc.constructor.name}`);
    let counter = 0;
    constructorFunc.prototype[methodName] = function (...args) {
        if (counter % 1000 === 0) {
            // tslint:disable:no-console
            console.log("Warning !", `${constructorFunc.name}#${methodName}`, " is now deprecated");
            console.log("         ", helpString);
        }
        counter++;
        return oldMethod.call(this, ...args);
    };
}
//# sourceMappingURL=set_deprecated.js.map