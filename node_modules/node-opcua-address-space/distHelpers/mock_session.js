"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mockSession = exports.MockContinuationPointManager = void 0;
exports.makeMockSessionContext = makeMockSessionContext;
const node_opcua_nodeid_1 = require("node-opcua-nodeid");
const node_opcua_types_1 = require("node-opcua-types");
const __1 = require("..");
class MockContinuationPointManager {
    registerHistoryReadRaw(_maxElements, _dataValues, _cnt) {
        throw new Error("Method not implemented.");
    }
    getNextHistoryReadRaw(_numValues, _cnt) {
        throw new Error("Method not implemented.");
    }
    registerReferences(_maxElements, _values, _cnt) {
        throw new Error("Method not implemented.");
    }
    getNextReferences(_numValue, _cnt) {
        throw new Error("Method not implemented.");
    }
    dispose() {
        // do nothing
    }
}
exports.MockContinuationPointManager = MockContinuationPointManager;
exports.mockSession = {
    getSessionId() {
        return new node_opcua_nodeid_1.NodeId();
    },
    continuationPointManager: new MockContinuationPointManager()
};
/**
 * Build a SessionContext that simulates a remote Session on a SecureChannel.
 *
 * Writing this by hand means assembling an ISessionBase and an IChannelBase and casting
 * through `any`; every field omitted silently reads back as undefined, which is not the
 * same as a permissive default. Use this instead:
 *
 * ```ts
 * const context = makeMockSessionContext({
 *     userName: "admin",
 *     securityMode: MessageSecurityMode.SignAndEncrypt,
 *     server
 * });
 * ```
 *
 * For an in-process caller with no Session at all, use SessionContext.defaultContext —
 * that one is granted every permission by design.
 */
function makeMockSessionContext(options) {
    const { userName, userIdentityToken, securityMode = node_opcua_types_1.MessageSecurityMode.None, securityPolicy = "", clientCertificate = null, channel, continuationPointManager = new MockContinuationPointManager(), server, object, endpointUrl } = options || {};
    const makeChannel = () => {
        if (channel !== undefined) {
            return channel ?? undefined; // null means: no SecureChannel at all
        }
        return {
            clientCertificate,
            securityMode,
            securityPolicy,
            getTransportSettings() {
                return { maxMessageSize: 0 };
            }
        };
    };
    const makeIdentityToken = () => {
        if (userIdentityToken !== undefined) {
            return userIdentityToken;
        }
        if (userName === undefined) {
            return undefined; // a Session that was never activated
        }
        return userName === "anonymous" ? new node_opcua_types_1.AnonymousIdentityToken() : new node_opcua_types_1.UserNameIdentityToken({ userName });
    };
    const session = {
        getSessionId() {
            return node_opcua_nodeid_1.NodeId.nullNodeId;
        },
        continuationPointManager,
        userIdentityToken: makeIdentityToken(),
        channel: makeChannel(),
        getEndpointUrl() {
            return endpointUrl;
        }
    };
    return new __1.SessionContext({ session, server, object });
}
//# sourceMappingURL=mock_session.js.map